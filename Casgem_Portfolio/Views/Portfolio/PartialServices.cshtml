@model List<Casgem_Portfolio.Models.Entities.TblService>

<section class="w3l-services">
    <div class="blog py-5" id="services">
        <div class="container py-lg-5">
            <h5 class="title-small text-center">Hizmetlerim</h5>
            <h3 class="title-big text-center mb-sm-5 mb-4">Senin için hangi hizmeti sağlayabilirim ? </h3>
            <div class="row">
                <div class="col-md-12 mx-auto pr-2">
                    <div class="owl-two owl-carousel owl-theme">
                        @foreach (var item in Model)
                        {
                            <div class="item">
                                <div class="card">
                                    <div class="box-wrap">
                                        <div class="icon">
                                            <span class="@item.ServiceIcon"></span>
                                        </div>
                                        <h4 class="number">@item.ServiceNumber</h4>
                                        <h4>@item.ServiceTitle.</h4>
                                        <p>
                                            @item.ServiceContect
                                        </p>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
            <div class="mt-5 text-more">
                <p class="mt-4 pt-3 sample text-center">
                    <a href="services.html">View All Services <span class="pl-2 fa fa-long-arrow-right"></span></a>
                </p>
            </div>
        </div>
    </div>
</section>
